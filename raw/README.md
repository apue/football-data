# Raw PMSR Cache

Put locally downloaded FIFA PMSR PDFs under the competition subdirectory when running the extractor offline, for example:

```text
raw/fifa-world-cup-2026/PMSR-M28-MEX-V-KOR.pdf
```

`raw/*.pdf` is ignored by git because original PMSR documents are not redistributed by this repository.
