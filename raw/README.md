# Raw PMSR Cache

Put locally downloaded FIFA PMSR PDFs in this directory when running the extractor offline.

`raw/*.pdf` is ignored by git because original PMSR documents are not redistributed by this repository.

